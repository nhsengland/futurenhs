import { formTypes } from '@constants/forms'
import { FormConfig } from '@appTypes/form'

export const deleteGroupMemberForm: FormConfig = {
    id: formTypes.DELETE_GROUP_MEMBER,
    steps: [
        {
            fields: [],
        },
    ],
}
