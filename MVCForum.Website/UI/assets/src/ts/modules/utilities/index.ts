import { registerServiceWorker } from '@modules/utilities/sw';

export const utilitiesInit = () => {

    /**
     * Init the service worker
     */
    // registerServiceWorker(); // Enable this once scope and rules for local dev known

}