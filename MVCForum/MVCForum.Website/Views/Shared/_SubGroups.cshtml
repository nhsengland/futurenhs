@model MvcForum.Web.ViewModels.Group.SubGroupViewModel
@{

}
    <div class="subGroupholder">
        @*<h4>@Html.Raw(string.Concat(Html.LanguageString("Group.SubGroupsHeading"), " ", Model.Parent_GroupId.Name))</h4>*@
        <ul class="clearfix">
            @foreach (var Group in Model.AllPermissionSets.Keys)
            {
                var permissions = Model.AllPermissionSets[Group];    
                if (!permissions[ForumConfiguration.Instance.PermissionDenyAccess].IsTicked)            
                {        
                    <li><a style="border-color: @(string.IsNullOrEmpty(Group.Colour) ? "#ced1d1" : Group.Colour)" href="@Html.Raw(Group.NiceUrl)">@Html.Raw(@Group.Name)</a></li>
                }
            }
        </ul>
    </div>

 