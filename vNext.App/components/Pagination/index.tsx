import { RichText } from '@components/RichText';

import { Props } from './interfaces';

export const Pagination: (props) => JSX.Element = ({
    content
}) => {

    return (

        <div className="c-pagination"> 
            TODO: Pagination   
        </div>

    )

}
